# Copy to .env for local dev. On Render, set these in the dashboard.
# The landing page has no Stripe keys or backend; checkout and signup are handled by the Muscle Up API.

# API base URL (no trailing slash). Required for Buy CTA and thank-you page.
MUSCLEUP_API_URL=https://muscleup-api.onrender.com

# Optional: "Already bought?" link to the Tracker app (e.g. set-password / login URL).
TRACKER_APP_URL=https://muscleup-web.onrender.com
